<?xml version="1.0" encoding="UTF-8"?>
<Frame xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="HomeAssistant.View.RoomCardView" 
    BackgroundColor="FloralWhite" BorderColor="Transparent" CornerRadius="20" Padding="0" HasShadow="True">

    <Frame.GestureRecognizers>
        <TapGestureRecognizer Command="{Binding SelectRoomCommand}" CommandParameter="{Binding Name}" Tapped="tapGestureRecognizer_Tapped"/>
    </Frame.GestureRecognizers>

    <Frame.Content>
        <Grid>
            <Image Aspect="AspectFill" Source="{Binding Background}"/>
            <Frame Margin="40" BackgroundColor="FloralWhite" CornerRadius="20" Opacity="0.7" HasShadow="False"/>
            <Label Text="{Binding Name}" TextColor="Black" FontSize="Title" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
        </Grid>
    </Frame.Content>
</Frame>